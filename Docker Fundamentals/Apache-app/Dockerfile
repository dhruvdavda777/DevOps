# Use the official Apache HTTP Server image
FROM httpd:2.4

# Copy our HTML file into Apache's default web root
COPY index.html /usr/local/apache2/htdocs/

# Apache serves on port 80 by default
EXPOSE 80
